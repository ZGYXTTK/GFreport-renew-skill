# 一次性确认清单（Step 4 用）

> 必填 ≤5 条，超出转建议项；用户可「一键全采」（接受所有默认值）。
> 「≥95% 把握」= 所有必填项已获明确答复。

## 必填项（默认 ✅ 一键全采）

| # | 问题 | 默认 | 用户批注 |
| --- | --- | --- | --- |
| 1 | 目标时点 = `config/时点对齐.json` 中的「目标时点」？ | ✅ | |
| 2 | 标的池 = `config/标的池.json`？新增/移除已在本期单独通知？ | ✅ | |
| 3 | 行业包名 = `--pack` 参数（如 `_default`/`aerospace`）？ | ✅ | |
| 4 | 输出目录前缀 = `--product`（默认「行业月报」）？ | ✅ | |
| 5 | 工作区探测优先级：`--anchor` > `--ws` > 环境变量 > 自动 | ✅ | |

## 建议项（仅在偏离默认时确认）

| # | 项目 | 备注 |
| --- | --- | --- |
| S1 | 通道降级策略（默认按 `权威源映射.json` 的「降级链」） | |
| S2 | 旧有「盲审不可达」章节是否保留 | |
| S3 | 跨月对齐阈值（默认 3 项叶子级差异暂停） | |
| S4 | 是否启用 `--resume` 断点续跑（默认 True） | |

## 回答记录

确认人：________________  时间：________________

签名 / 一键全采：`□ 已确认全部默认`